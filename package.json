{
  "scripts": {
    "start": "node app/server.js",
    "setup": "fly postgres connect -a marvelous-moviesdb < setup.sql",
    "start:local": "env-cmd node app/server.js",
    "setup:local": "env-cmd psql -d postgres -c 'create database marvelous-movies' -f setup.sql"
  },
  "dependencies": {
    "env-cmd": "^10.1.0",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.11"
  }
}
